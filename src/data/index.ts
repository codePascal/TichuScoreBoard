/** Barrel re-export for the data layer (database init + repositories).
 *  Import anywhere with: import { initDatabase, getAllPlayers } from '../data'
 */
export * from './database';
export * from './repositories/PlayerRepository';
